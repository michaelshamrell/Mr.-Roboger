Describe: roboger array

test:  a function that takes the user input and puts it into an array loop, defining the loop with "numbers"
code: const array = function(input) {
  let numbers = [];
}
expected output: when the user inputs a number the fuction(input) should run the numbers loop.
